enum Panel {
  info,
  addTree,
  addRow,
  addForest,
  resetGeometry,
  delete,
  map,
  none,
}

// ENUMS FOR EDITING
enum EditorType {
  point,
  line,
  polygon,
}

enum EditorAction {
  add,
  reset,
  pop,
}

enum LanguangeSpecies {
    scientific,
    english,
    italian,
}

enum NumberType { integer, double }
